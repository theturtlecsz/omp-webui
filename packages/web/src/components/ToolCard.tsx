import { useEffect, useState } from 'react';
import { CheckCircle2, ChevronDown, CircleX, Clock3, Copy, LoaderCircle, XCircle } from 'lucide-react';
import { resolveToolRenderer, type ToolRenderState } from '../tool-render/registry';
import type { GenericToolModel } from '../tool-render/generic-model';
import type { ToolCard as Card } from '../lib/types';
const toRendererState=(state:Card['state']):ToolRenderState=>state==='success'?'completed':state==='failure'?'error':state;
const elapsed=(started?:number,ended?:number)=>started?`${(((ended??Date.now())-started)/1000).toFixed(1)} s`:'';
export function ToolCard({card,onFilePath}:{card:Card;onFilePath?:(path:string)=>void}){const [open,setOpen]=useState(!card.collapsed);const [now,setNow]=useState(Date.now());useEffect(()=>{if(card.state!=='running')return;const timer=setInterval(()=>setNow(Date.now()),500);return()=>clearInterval(timer)},[card.state]);const model=resolveToolRenderer(card.toolName)({toolCallId:card.toolCallId,toolName:card.toolName,args:card.args,state:toRendererState(card.state),partialResult:card.partialResult,result:card.result,isError:card.isError,startedAt:card.startedAt,endedAt:card.endedAt}) as GenericToolModel;const status=card.state==='running'?'Running':card.state==='success'?'Succeeded':card.state==='failure'?'Failed':'Cancelled';const Icon=card.state==='running'?LoaderCircle:card.state==='success'?CheckCircle2:card.state==='failure'?CircleX:XCircle;const copy=()=>navigator.clipboard?.writeText(model.displayText);const paths=Object.values((card.args&&typeof card.args==='object'?card.args:{}) as Record<string,unknown>).filter((v):v is string=>typeof v==='string'&&(v.includes('/')||v.includes('\\')));return <article className={`tool-card tool-card--${card.state}`} aria-labelledby={`tool-${card.toolCallId}`}><div className="tool-card__header"><Icon size={16} className={card.state==='running'?'spin':undefined}/><div className="tool-card__title"><h3 id={`tool-${card.toolCallId}`}>{card.toolName}</h3><span>{status}</span></div><span className="tool-card__elapsed u-tabular-nums">{elapsed(card.startedAt,card.endedAt??now)}</span><button aria-expanded={open} aria-controls={`tool-detail-${card.toolCallId}`} aria-label={`${open?'Collapse':'Expand'} ${card.toolName} details`} onClick={()=>setOpen(!open)}><ChevronDown size={16}/></button></div><p className="tool-card__summary">{model.errorText ?? (model.displayText.split('\n')[0] || 'No output reported.')}</p>{paths.length>0&&<div className="tool-card__paths">{paths.map(path=><button key={path} onClick={()=>onFilePath?.(path)}><code>{path}</code></button>)}</div>}{open&&<div id={`tool-detail-${card.toolCallId}`} className="tool-card__details"><div className="tool-card__actions"><button onClick={copy}><Copy size={14}/> Copy output</button></div><strong>Arguments</strong><pre><code>{model.prettyArgs}</code></pre><strong>{model.errorText?'Error output':'Output'}</strong><pre><code>{model.displayText||model.errorText||'No output.'}</code></pre><details><summary>Raw JSON</summary><pre><code>{model.rawJson}</code></pre></details></div>}</article>}
