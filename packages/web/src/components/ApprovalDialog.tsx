import { useRef } from 'react';
import { ShieldCheck } from 'lucide-react';
import type { PendingInteraction } from '../lib/types';
import { TimeoutCountdown, useFocusTrap } from './dialog-utils';
export function ApprovalDialog({ interaction, onRespond }: { interaction: PendingInteraction; onRespond: (confirmed: boolean) => void }) { const ref = useRef<HTMLDivElement>(null); const p = interaction.payload; const confirm = () => onRespond(true); useFocusTrap(ref, () => onRespond(false), confirm); return <div className="modal-backdrop"><div className="modal" ref={ref} role="dialog" aria-modal="true" aria-labelledby="approval-title" aria-describedby="approval-detail"><ShieldCheck size={24} /><h2 id="approval-title">Allow OMP to continue?</h2><p id="approval-detail">{String(p.message ?? p.title ?? 'This action requires your approval.')}</p>{Boolean(p.command || p.path) && <pre><code>{String(p.command ?? p.path)}</code></pre>}<TimeoutCountdown timeout={p.timeout} /><div className="modal__actions"><button className="button button--quiet" onClick={() => onRespond(false)}>Deny</button><button className="button button--primary" onClick={confirm}>Allow once</button></div></div></div>; }
