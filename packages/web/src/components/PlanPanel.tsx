import { useState } from 'react';
import { CheckCircle2, Circle, CircleDot } from 'lucide-react';
type Task={content?:string;title?:string;status?:string};type Phase={name?:string;tasks?:Task[]};
export function PlanPanel({todos}:{todos?:unknown}){const [open,setOpen]=useState(true);const phases=Array.isArray(todos)?todos as Phase[]:[];return <section className="panel" aria-label="Plan"><header><h2>Plan</h2><button aria-expanded={open} onClick={()=>setOpen(!open)}>{open?'Collapse':'Expand'}</button></header>{open&&(phases.length?<ol className="plan-list">{phases.map((phase,i)=><li key={`${phase.name}-${i}`}><strong>{phase.name??`Phase ${i+1}`}</strong><ol>{phase.tasks?.map((task,j)=>{const state=task.status??'pending';const Icon=state==='completed'?CheckCircle2:state==='in_progress'?CircleDot:Circle;return <li key={j}><Icon size={15}/><span>{task.content??task.title??'Untitled task'}</span><small>{state.replace('_',' ')}</small></li>})}</ol></li>)}</ol>:<p className="empty-panel">No plan yet. It will appear when OMP creates one.</p>)}</section>}
